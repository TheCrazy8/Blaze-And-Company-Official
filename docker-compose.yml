version: "3.8"
services:
  device-backend:
    build: .
    ports:
      - "4000:4000"
    environment:
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - FRONTEND_ORIGIN=${FRONTEND_ORIGIN}
    restart: unless-stopped
